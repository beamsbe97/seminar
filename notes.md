## contrastive loss

# constrastive loss for prompt selection
- check if performance varies too much if we use less similar candidate prompts
(basically does it matter if we optimise prompt selection)

# contrastive loss to align condenser's output with query
- might be just a different formulation of the alr existing consine similarity
(since the condensed prompt is alr supposed to be similar to query)